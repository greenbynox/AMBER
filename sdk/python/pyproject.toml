[project]
name = "ember-sdk"
version = "0.1.0"
description = "SDK Python minimal pour EMBER"
readme = "README.md"
requires-python = ">=3.8"
license = { text = "AGPL-3.0" }

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
