# Modern UI (SPA)

Minimal HTML/CSS/JS interface served by the API.

## Access
- `/app`

## Auth
The form asks for `project` and `key`.

## Features
- Issue list
- Issue detail
- Filters (status, level, search)
