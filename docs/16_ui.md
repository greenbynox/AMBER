# Minimal UI

Simple HTML (server‑side) interface to browse issues.

## Access
- `/ui?project=<id>&key=<api_key>`
- `/ui/issues/:id?project=<id>&key=<api_key>`

## Filters
Optional parameters:
- `status` (open|resolved|ignored)
- `level` (error|warning|info|debug)
- `q` (search)

> Note: this mode is intentionally simple (dev/local). A dedicated frontend UI will follow.
